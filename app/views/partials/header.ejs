<nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
    <a class="navbar-brand" href="/">
        Node CMS
    </a>
    <div class="ms-auto">
        <div class="d-flex align-items-center gap-3">
            <% if (user) { %>
                <div class="d-flex align-items-center gap-3">
                    <p class="mb-0"><%= user.email %></p>
                    <% if (user.avatar) { %>
                        <img src="data:image/png;base64,<%= user.avatar %>" alt="Avatar" class="rounded-circle"
                             width="40"
                             height="40">
                    <% } else { %>
                        <img src="/images/avatar.png" alt="Avatar" class="rounded-circle" width="40"
                             height="40">
                    <% } %>
                </div>
            <% } %>
            <a role="link" href="/create-post" class="btn btn-outline-primary" data-bs-toggle="tooltip"
               title="New post">
                <i class="bi bi-file-post"></i>
            </a>
            <a role="link" href="/logout" class="btn btn-outline-danger" data-bs-toggle="tooltip" title="Logout">
                <i class="bi bi-box-arrow-left"></i>
            </a>
        </div>
    </div>
</nav>